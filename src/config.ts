// Small site-wide configuration. Keep values that toggle optional UI here so a
// single edit flips them on/off across the site.
export const siteConfig = {
  // URL of the launch announcement post. Leave empty until it is published —
  // while empty, the "Read the announcement" line does not render at all
  // (no dead "#" link).
  announcementUrl: "",
};
